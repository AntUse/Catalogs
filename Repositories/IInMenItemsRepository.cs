using catalogs.Entities;
using System;
using System.Collections.Generic;

//namespace Catalogs.Repositories
//{
    //this is the interface generated from the InMenItemsRepository. To easily generate an interface, from a class. click the class and click the bulb light.
    // this will request to create an interface in a new file(class.cs) and also the references you want. The referencing is collected from the basic interface i created ealier on the InMen.. class

    //public interface IInMenItemsRepository
    //{
       // Item GetItem(Guid Id);
      //  IEnumerable<Item> GetItems();

       // void CreatedItem(Item item);
       // void UpdateItem(Item item);
       // void DeleteItem(Guid id);
  //  }
//}